UMC Bell Extension FINAL v17

- Persistent login: only explicit Extension Logout can log out.
- Manual audio actions never change authentication state.
- Login form uses standard username/current-password autocomplete fields for browser password-manager compatibility.
- One Schedule show/hide eye control only.
- Main App Settings icon/panel removed; Reset Bell and Reset Schedule remain inside Admin Profile.
- Background alarms/offscreen audio remain active after the main app tab is closed.


## v21 updates
- Main App floating status shows live Online/Offline state.
- Main App floating status includes a manual Sync icon.
- Manual Sync uses the extension bridge safely and performs app-to-extension then extension-to-app synchronization.
- Main App automatically publishes network state and current schedule while open.
